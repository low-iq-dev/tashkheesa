{
  "name": "tashkheesa-portal",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "sh -c \"P=${PORT:-3000}; PID=$(lsof -tiTCP:$P -sTCP:LISTEN 2>/dev/null); if [ -n \\\"$PID\\\" ]; then echo \\\"🧹 Freeing port $P (PID $PID)\\\"; kill -9 $PID; fi; nodemon src/server.js\"",
    "dev:nokill": "PORT=3000 nodemon src/server.js",
    "doctor": "node scripts/doctor.js",
    "smoke": "node scripts/smoke.js",
    "backup:db": "node scripts/backup-db.js",
    "db:integrity": "node scripts/db-integrity.js",
    "preflight": "node scripts/preflight.js",
    "verify": "npm run deps:check && node scripts/verify.js",
    "deps:check": "node -e \"const fs=require('fs'); if(!fs.existsSync('node_modules')){ console.error('⛔ node_modules missing. Run: npm i'); process.exit(1);} console.log('✅ deps ok');\"",
    "safe": "npm run verify",
    "verify:offline": "SKIP_SMOKE=1 npm run verify",
    "safe:offline": "npm run verify:offline",
    "backups:list": "node -e \"const fs=require('fs'); const path=require('path'); const dir=path.resolve('backups'); if(!fs.existsSync(dir)){ console.log('No backups directory'); process.exit(0);} const files=fs.readdirSync(dir).filter(f=>f.endsWith('.db')).sort().reverse(); if(!files.length){ console.log('No .db backups found in backups/'); process.exit(0);} console.log('Backups (newest first):'); files.slice(0,20).forEach(f=>console.log(' - '+f));\"",
    "rollback:db": "node -e \"const fs=require('fs'); const path=require('path'); const arg=process.argv[2]; if(!arg){ console.error('Usage: npm run rollback:db -- <backup-file.db>'); process.exit(1);} const backupsDir=path.resolve('backups'); const backupPath=path.join(backupsDir,arg); const dbPath=path.resolve('data/portal.db'); if(!fs.existsSync(backupPath)){ console.error('Backup not found: '+backupPath); process.exit(1);} if(!fs.existsSync(dbPath)){ console.error('DB not found: '+dbPath); process.exit(1);} const ts=new Date().toISOString().replace(/[:.]/g,'-'); const safety=path.join(backupsDir,'portal-pre-rollback-'+ts+'.db'); fs.copyFileSync(dbPath,safety); fs.copyFileSync(backupPath,dbPath); console.log('✅ Rolled back DB to '+arg); console.log('✅ Safety copy of previous DB saved as '+path.basename(safety));\"",
    "test": "node tests/run.js",
    "test:addons": "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node --test --test-concurrency=1 src/services/addons/__tests__/*.test.js",
    "test:addons:lifecycle": "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node scripts/test_addon_lifecycle.js",
    "addons:seed":     "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node scripts/seed_addon_test_traffic.js --env=local",
    "addons:seed:cleanup": "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node scripts/seed_addon_test_traffic.js --env=local --cleanup",
    "addons:parity":   "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node scripts/verify_addon_parity.js --env=local --prefix=parity-fixture- --since=1970-01-01T00:00:00Z",
    "addons:rollback-rehearsal": "PG_SSL=false DATABASE_URL=${TEST_DATABASE_URL:-postgresql://ziadelwahsh@localhost:5432/tashkheesa} node scripts/rollback_rehearsal.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@aws-sdk/client-s3": "^3.1030.0",
    "@aws-sdk/s3-request-presigner": "^3.1030.0",
    "bcryptjs": "^3.0.3",
    "cloudinary": "^2.9.0",
    "cookie-parser": "^1.4.7",
    "dayjs": "^1.11.19",
    "dotenv": "^17.2.3",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-async-errors": "^3.1.1",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "handlebars": "^4.7.8",
    "helmet": "^8.1.0",
    "image-size": "^1.2.1",
    "jsonwebtoken": "^9.0.3",
    "morgan": "^1.10.1",
    "multer": "^2.0.2",
    "node-cron": "^4.2.1",
    "node-fetch": "^2.7.0",
    "nodemailer": "^8.0.1",
    "openai": "^6.22.0",
    "pdf-parse": "^2.4.5",
    "pdfkit": "^0.17.2",
    "pg": "^8.18.0",
    "pg-boss": "^12.14.0",
    "twilio": "^5.12.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.24",
    "concurrently": "^9.2.1",
    "nodemon": "^3.0.2",
    "postcss": "^8.5.6"
  }
}
