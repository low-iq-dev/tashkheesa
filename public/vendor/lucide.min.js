(function (global) {
  'use strict';

  var NS = 'http://www.w3.org/2000/svg';

  var ICONS = {
    'clipboard-list': [
      ['rect', { x: '8', y: '2', width: '8', height: '4', rx: '1', ry: '1' }],
      ['path', { d: 'M8 4H6a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h8' }],
      ['path', { d: 'M13 12h6' }],
      ['path', { d: 'M13 16h6' }],
      ['path', { d: 'M13 20h6' }]
    ],
    briefcase: [
      ['rect', { x: '2', y: '7', width: '20', height: '13', rx: '2', ry: '2' }],
      ['path', { d: 'M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2' }],
      ['path', { d: 'M2 13h20' }]
    ],
    'check-circle-2': [
      ['circle', { cx: '12', cy: '12', r: '10' }],
      ['path', { d: 'm9 12 2 2 4-4' }]
    ],
    bell: [
      ['path', { d: 'M10.268 21a2 2 0 0 0 3.464 0' }],
      ['path', { d: 'M3.262 15.326A1 1 0 0 0 4 17h16a1 1 0 0 0 .74-1.674C19.41 13.956 18 12.499 18 8a6 6 0 0 0-12 0c0 4.499-1.411 5.956-2.738 7.326' }]
    ],
    clock: [
      ['circle', { cx: '12', cy: '12', r: '10' }],
      ['path', { d: 'M12 6v6l4 2' }]
    ],
    scan: [
      ['path', { d: 'M3 7V5a2 2 0 0 1 2-2h2' }],
      ['path', { d: 'M17 3h2a2 2 0 0 1 2 2v2' }],
      ['path', { d: 'M21 17v2a2 2 0 0 1-2 2h-2' }],
      ['path', { d: 'M7 21H5a2 2 0 0 1-2-2v-2' }],
      ['path', { d: 'M7 12h10' }]
    ],
    'heart-pulse': [
      ['path', { d: 'M19.5 13.5 12 21l-7.5-7.5a5 5 0 1 1 7.5-6.5 5 5 0 1 1 7.5 6.5z' }],
      ['path', { d: 'M3 12h4l2-3 2 6 2-4h8' }]
    ],
    sparkles: [
      ['path', { d: 'M12 3l1.5 3.5L17 8l-3.5 1.5L12 13l-1.5-3.5L7 8l3.5-1.5L12 3z' }],
      ['path', { d: 'M5 14l.75 1.75L7.5 16.5l-1.75.75L5 19l-.75-1.75L2.5 16.5l1.75-.75L5 14z' }],
      ['path', { d: 'M19 14l.5 1.2 1.2.5-1.2.5-.5 1.2-.5-1.2-1.2-.5 1.2-.5z' }]
    ],
    activity: [
      ['path', { d: 'M22 12h-4l-3 9-6-18-3 9H2' }]
    ],
    'flask-conical': [
      ['path', { d: 'M10 2v7.31' }],
      ['path', { d: 'M14 9.3V2' }],
      ['path', { d: 'M8.5 2h7' }],
      ['path', { d: 'M14 9.3a6 6 0 1 1-4 0' }],
      ['path', { d: 'M5.58 16.5h12.85' }]
    ],
    brain: [
      ['path', { d: 'M12 18a4 4 0 0 0 4-4V9a4 4 0 1 0-8 0v5a4 4 0 0 0 4 4z' }],
      ['path', { d: 'M8 10a3 3 0 0 0-3 3v1a3 3 0 0 0 3 3' }],
      ['path', { d: 'M16 10a3 3 0 0 1 3 3v1a3 3 0 0 1-3 3' }],
      ['path', { d: 'M12 7v11' }]
    ],
    bone: [
      ['path', { d: 'M17 10a2.828 2.828 0 1 0-4-4L7 12a2.828 2.828 0 1 0 4 4l6-6z' }]
    ],
    baby: [
      ['circle', { cx: '12', cy: '7', r: '2' }],
      ['path', { d: 'M9 21v-5a3 3 0 0 1 6 0v5' }],
      ['path', { d: 'M8 14h8' }],
      ['path', { d: 'M12 9v4' }]
    ],
    ear: [
      ['path', { d: 'M6 9a6 6 0 1 1 12 0c0 3-2 4-3 5.5-1 1.3-1 2.5-1 3.5' }],
      ['path', { d: 'M10 20a2 2 0 0 0 4 0' }]
    ],
    eye: [
      ['path', { d: 'M2 12s3.5-6 10-6 10 6 10 6-3.5 6-10 6-10-6-10-6Z' }],
      ['circle', { cx: '12', cy: '12', r: '3' }]
    ],
    'briefcase-medical': [
      ['rect', { x: '2', y: '7', width: '20', height: '13', rx: '2', ry: '2' }],
      ['path', { d: 'M16 7V5a2 2 0 0 0-2-2h-4a2 2 0 0 0-2 2v2' }],
      ['path', { d: 'M12 11v6' }],
      ['path', { d: 'M9 14h6' }]
    ],
    'circle-help': [
      ['circle', { cx: '12', cy: '12', r: '10' }],
      ['path', { d: 'M9.1 9a3 3 0 1 1 5.8 1c-.7.9-1.9 1.5-1.9 3' }],
      ['path', { d: 'M12 17h.01' }]
    ]
  };

  function toArray(list) {
    return Array.prototype.slice.call(list || []);
  }

  function uniqueClasses(classNames) {
    var seen = Object.create(null);
    var out = [];
    String(classNames || '')
      .split(/\s+/)
      .filter(Boolean)
      .forEach(function (name) {
        if (!seen[name]) {
          seen[name] = true;
          out.push(name);
        }
      });
    return out.join(' ');
  }

  function getAttributes(el) {
    var attrs = {};
    toArray(el && el.attributes).forEach(function (attr) {
      attrs[attr.name] = attr.value;
    });
    return attrs;
  }

  function createSvg(name, attrs) {
    var icon = ICONS[name] || ICONS['circle-help'];
    var svg = document.createElementNS(NS, 'svg');

    var defaults = {
      xmlns: NS,
      width: '24',
      height: '24',
      viewBox: '0 0 24 24',
      fill: 'none',
      stroke: 'currentColor',
      'stroke-width': '2',
      'stroke-linecap': 'round',
      'stroke-linejoin': 'round'
    };

    Object.keys(defaults).forEach(function (key) {
      svg.setAttribute(key, defaults[key]);
    });

    var className = uniqueClasses('lucide lucide-' + name + ' ' + (attrs.class || ''));
    if (className) svg.setAttribute('class', className);

    Object.keys(attrs).forEach(function (key) {
      if (key === 'data-lucide' || key === 'class') return;
      svg.setAttribute(key, attrs[key]);
    });

    icon.forEach(function (node) {
      var tag = node[0];
      var nodeAttrs = node[1];
      var child = document.createElementNS(NS, tag);
      Object.keys(nodeAttrs).forEach(function (key) {
        child.setAttribute(key, nodeAttrs[key]);
      });
      svg.appendChild(child);
    });

    return svg;
  }

  function createIcons(options) {
    var opts = options || {};
    var root = opts.root || document;
    var placeholders = toArray(root.querySelectorAll('[data-lucide]'));

    placeholders.forEach(function (el) {
      var name = String(el.getAttribute('data-lucide') || '').trim();
      if (!name) return;
      var attrs = getAttributes(el);
      var svg = createSvg(name, attrs);
      if (el.parentNode) {
        el.parentNode.replaceChild(svg, el);
      }
    });

    return placeholders.length;
  }

  var api = {
    createIcons: createIcons,
    icons: ICONS,
    version: 'local-vendor-1.0.0'
  };

  global.lucide = Object.assign({}, global.lucide || {}, api);
})(typeof window !== 'undefined' ? window : this);
