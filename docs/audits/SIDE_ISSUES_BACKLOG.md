# Side Issues Backlog

Single canonical ledger of named side issues raised across audit sessions.
Created 2026-05-12 (backfill request post Theme 10b scoping + side issues
#47–#56 batch). Maintained going forward — every new side issue gets a row.

## Schema

| Column | Meaning |
|---|---|
| `#` | Side issue number. Sequence is global, not per-theme. |
| `Status` | `OPEN` (not started), `RESOLVED` (shipped), `DEFERRED` (decision made not to ship now). |
| `Title` | One-line description. Match the title used in commit messages and audit docs. |
| `Source` | Theme + phase that surfaced it, or `ad-hoc` for items raised outside an audit. |
| `Severity` | `P0` (launch-blocking), `P1` (must-have but not blocking), `P2` (should-have), `P3` (nice-to-have). |
| `Blocker` | External dependency before resolution can proceed: `Meta` (WhatsApp verification), `Internal-policy` (waiting on operator decision or product capability migration), `None`. |
| `Notes` | One-line summary + commit SHA(s) if resolved. |

## Theme 10b execution status (2026-05-12)

Theme 10b RTL fixes shipped in 7 atomic commits per the audit's 8-section
plan at `docs/audits/THEME_10B_RTL_AUDIT_FIX_PLAN.md` (commit `8cd6f1d`):

| Phase | Sub-issue | SHA | Summary |
|---|---|---|---|
| 1 | F — Arabic font fallback | `6456299` | Inter + Noto Sans Arabic unicode-range mix across 6 CSS surfaces + 5 Google Fonts loaders |
| 2 | C — Locale-aware formatters | `30ea413` | `src/utils/formatNumber.js` + 6 patient-facing bare-callsite fixes + OQ-3 notification dates |
| 3 | D — LTR-lock inputs | `e93924b` | 56 `dir="ltr"` insertions across 33 views + safety-net CSS rule |
| 4 | B — Icon mirroring | `8feef60` | Promoted `.p-icon--flip` globally + classed 9 directional SVGs |
| 5 | A — Layout primitives | `7d9de1c` | 76 logicalisations across 12 CSS files (margin/padding/border-l/r → inline-start/end) |
| 6 | E — Chart.js cosmetic RTL | `3b7a8ff` | `direction` config on 7 admin analytics charts |
| 7 | Tests T1-T6 | `f851ebb` | Lint + HTTP regression suite (5 lint + 1 boot test) |

Three sub-issues raised follow-up work during execution (rows #57, #58,
#60 below). #59 not assigned in this batch — surfaced as a gap so future
work can claim it without renumbering.

## Backfill scope note

**#1–#42: not backfilled in this commit.** A `git log` sweep across the
full history shows the `Side issue #N` numbering convention first appears
at **#48** (3 pricing-reset commits, `108fcf0 / d26bd89 / 08d46b9`) and at
**#47** (referenced in commit-body text but no commit attributed to it).
Earlier numbers may live in conversation history or an external tracker
that isn't in the repo. Per "don't invent rows," #1–#42 are intentionally
absent. If you want them in the ledger, paste titles + status and they get
appended.

**#52 ≡ #56 duplicate.** Both rows in the input list carry the title
"Migrate Uploadcare File Uploader 3.x → Blocks v1.x." Surfaced and resolved
in the table below: #52 holds the canonical row (DEFERRED, source =
side issue #52 today), and #56 is logged as a duplicate pointing at #52.
Recommend the next time you touch this list, drop #56 and renumber any
subsequent rows.

## Ledger

| # | Status | Title | Source | Severity | Blocker | Notes |
|---|---|---|---|---|---|---|
| 43 | RESOLVED | Doctor earnings clawback policy + `recomputeOnRefund` hook | Theme 7b follow-up | P2 | None | Resolved at `77903c5`. Policy decided 2026-05-12: `reason='sla_breach'` → full clawback (`earned_amount=0`); `reason IN ('patient_request','operator_refund')` AND earnings-row-exists (post-`ASSIGNED`) → doctor keeps 10% of `(baseShare + upliftShare)`; pre-acceptance → no-op (no earnings row to touch). Implementation: new `recomputeOnRefund(orderId, {reason})` in `src/services/earnings_writer.js` (Site 4 hook; decoupled from `recomputeOnBreach` which keeps its uplift-only intermediate behavior). Wired into `routes/superadmin.js` mark-paid handler. Migration `054_doctor_earnings_clawback.sql` adds `clawback_reason TEXT` + `clawback_applied_at TIMESTAMP` for audit + idempotency guard. 7 tests in `tests/services/recompute-on-refund.test.js` cover all 4 policy paths + idempotency + unknown-reason + wiring-lint. `docs/PAYOUT_AND_URGENCY_POLICY.md` §4.A documents the canonical rules. |
| 44 | RESOLVED | Operator-initiated refund creation from queue | Theme 7b audit OQ-14 | P2 | None | Resolved at `5fd96bf`. New routes `GET /superadmin/refunds/create?order_id=X` (form) + `POST /superadmin/refunds/create` (handler), new view `src/views/superadmin_refund_create.ejs`. INSERTs refunds row with `reason='operator_refund'`, `requested_by=req.user.id`, `status='pending'` always (skips auto-approve gate; operator IS the approver). Validates order exists + payment_status='paid' + no existing non-terminal refund row. Notifies patient via NEW template `patient_refund_opened_by_operator` (EN: "A refund has been opened on your behalf", AR: "تم فتح طلب استرداد نيابةً عنك") — honest copy, not reused patient-self template. `logOrderEvent({label: 'operator_refund_created'})` for audit. Admin fan-out intentionally skipped (operator IS the admin). UI: "Create refund for this order" button on prefill banner + top-of-queue "+ New refund" button (independent of prefill). No migration — every column already exists. |
| 45 | RESOLVED | `RESEND_API_KEY` documented + fail-fast in prod | Theme 4 Sub-issue A | P1 | None | Resolved at `096211a` (env validator + dead SMTP block kill). Operationally verified during pre-launch audit pass. |
| 46 | RESOLVED | Silent-failure gap: `max_retries_exceeded` in `notification_worker` doesn't emit `NOTIFICATION_DROPPED` case_event | Theme 8 Phase 3 follow-up | P2 | None | **Retitled 2026-05-12.** Original framing ("schema-drift from retry path") was speculative; investigation found no literal schema drift on `case_events`. The real bug: `notification_worker.js:300-322` (max-retries-exhausted branch) wrote to `error_logs` but did not emit a `NOTIFICATION_DROPPED` case_event, so `/ops/silent-failures` (which keys on `case_events` event_types ending in `_DROPPED`/`_FAILED`/`_SKIPPED`/`_NO_OP`) missed every notification that got enqueued cleanly and then failed all 3 retry attempts. The enqueue-side already emitted via `notify.emitNotificationDropped` (`notify.js:271,388,591,618,622,628`). Fix: export `emitNotificationDropped` from `notify.js`; call it from the max-retries branch with `reason='max_retries_exceeded'`. Code-only, no migration. See commit message in this commit. |
| 47 | RESOLVED | Delete `sla_worker.js` + `sla_watcher.js` + `jobs/sla_watcher.js` | ad-hoc (2026-05-12) | P3 | None | Shipped across 4 atomic commits 2026-05-12: (1) `6061045` removed the 3 live callers (server.js:212+968, superadmin.js:9+2552+2889; the `/superadmin/tools/run-sla-sweep` handler at 2889 was repointed to canonical `case_sla_worker.runCaseSlaSweep`) and deleted 2 broken scripts (`scripts/run_sla_check.js`, `scripts/run_sla_sweep.js` — both required non-existent or unexported symbols, zero callers). (2) `0a6f5dd` removed the `runWatcherSweep` await-check from `theme6-async-interval-pattern.test.js` and the export-shape assertion + dead-file allowlist from `theme7-sla-breach-uses-canonical.test.js`. (3) This commit deletes `src/sla_worker.js`, `src/sla_watcher.js`, `src/jobs/sla_watcher.js`; updates stale comments in `theme7b-notify-admins-shared.test.js` + `theme8-worker-errlog-coverage.test.js`; removes `src/sla_watcher.js` from the `scripts/verify.js` syntax-check array. All 4 affected test files green post-deletion. Investigation also surfaced original spec was 2 files but 3 actually existed (the `src/sla_watcher.js` actively-imported variant vs `src/jobs/sla_watcher.js` dead twin); operator-facing manual sweep route now uses canonical worker instead of the no-op stub. |
| 48 | RESOLVED | 95 unpriced services — v4 pricing reset | ad-hoc (pricing audit) | P0 | None | Resolved across migrations `051_insert_lab_panels.sql`, `052_price_and_unhide_launch.sql`, `053_delete_bundled_labs.sql`. Commits: `108fcf0` (7 new lab panel SKUs), `d26bd89` (price + un-hide 38 launch SKUs), `08d46b9` (hard-delete 38 lab tests). Companion docs at `docs/pricing/PRICING_RECONCILIATION_v4_PROD.md`. |
| 49 | RESOLVED | Worker heartbeats use canonical names | ad-hoc (2026-05-12) | P2 | None | Resolved at `440e988`. Renamed pingOps callers in `case_sla_worker.js` (`ops-agent` → `case_sla_worker`), `notification_worker.js` (`care-agent` → `notification_worker`), `instagram/scheduler.js` (`growth-agent` → `instagram_scheduler`). Phase 7 Widget 3 now keys on canonical names directly. |
| 50 | RESOLVED | `error_rate_5x` critical-alert cron | ad-hoc (2026-05-12) | P1 | None | Resolved at `1fcf472`. New `src/jobs/error_rate_check.js` runs the same baseline-vs-current query as `/ops` Widget 4 (7-day hourly baseline + current-hour count); fires `sendCriticalAlert(..., 'error_rate_5x')` when `current >= 5 && current >= 5 * baseline`. Cron registered at `*/15 * * * *` in `src/server.js`. Regression test at `tests/core/error-rate-cron-regression.test.js`. |
| 51 | DEFERRED | Migrate inline `<style>` blocks + `style=` attrs → nonced/external CSS | ad-hoc (2026-05-12) | P3 | Internal-policy | Promoted to **Theme 12** (Inline style migration) — multi-week scope. Today's grep: 37 views with inline `<style>` blocks + 115 with `style="..."` attrs. Removing `style-src 'unsafe-inline'` would break the UI; deferred until Theme 12 ships the bulk migration. Companion record at `7c32597` ("docs(audit): record helmet 'unsafe-inline' dependency for Theme 2"). |
| 52 | PROMOTED | Migrate Uploadcare File Uploader 3.x → Blocks v1.x | ad-hoc (2026-05-12); promoted to **Theme 13** (2026-05-12) | P2 | Internal-policy (see notes) | **Promoted to Theme 13 — Uploadcare → R2 migration** (`docs/audits/THEME_13_R2_MIGRATION_FIX_PLAN.md`). Theme 13 supersedes the original "migrate to Blocks v1.x" framing: instead of upgrading the Uploadcare SDK, the patient wizard widget is replaced with a direct-to-R2 multipart upload endpoint (R2 already handles every other upload surface — doctor photos, signatures, prescriptions, generated reports, `order_flow.js` patient case files). This **closes #52** because removing the dependency entirely is cleaner than migrating it. Pre-cutover Q6 prod query (2026-05-12) returned only 3 rows in `order_files` (all from the mobile API `uploadcare_uuid` path; zero from the wizard widget) — backfill scope shrinks from "migration script" to "5-minute manual cleanup" in Theme 13B. Sub-issue A (new POST endpoint, flag-gated on `UPLOAD_R2_DIRECT_ENABLED`) shipped 2026-05-12. |
| 53 | RESOLVED | `patient_payment_required.ejs` — video add-on checkbox UX when `VIDEO_CONSULTATION_ENABLED=false` | ad-hoc | P3 | None | Resolved at `2066fc8`. `src/routes/patient.js` GET `/portal/patient/pay/:id` handler now reads `isVideoEnabled()` once and threads `videoEnabled` into both render-call locals. When `videoEnabled === false`, the addon card in `patient_payment_required.ejs:167-189` renders the checkbox `disabled`, dims the card background, swaps the price colour to muted, and appends a "Coming soon / قريباً" pill next to the title. Patient now sees explicit affordance instead of having the addon submission silently dropped at `routes/payments.js:480`. Hidden fallback inputs (lines 312, 362) untouched — they already default to `value="0"` when checkbox is unchecked or disabled. |
| 54 | RESOLVED | `video_scheduler` + `acceptance_watcher` heartbeats | ad-hoc (2026-05-12, surfaced during #49) | P2 | None | Resolved at `9005575`. `src/video_scheduler.js` now defines `pingOps()` (same shape as case_sla_worker.js:503-512) and calls it at the end of the every-1-min cron tick (after all 3 sweeps complete). `src/workers/acceptance_watcher.js` defines the same helper and calls it at the end of every successful 2-min sweep with the count of expired orders processed. After this lands + first cron tick, Widget 3 + the CONFIGURED_AGENTS table (added by #55) show fresh `lastRun` for all 5 canonical workers instead of 2 stuck on "never run". |
| 55 | RESOLVED | `CONFIGURED_AGENTS` dashboard rendering goes stale post-rename | ad-hoc (2026-05-12, surfaced during #49) | P3 | None | Resolved in this commit. Updated `CONFIGURED_AGENTS` (`src/routes/ops.js:52-58`) and the scheduling table (`src/views/ops-dashboard.ejs:513-518`) to the 5 canonical worker names (`case_sla_worker`, `notification_worker`, `video_scheduler`, `instagram_scheduler`, `acceptance_watcher`). `video_scheduler` + `acceptance_watcher` will show "never run" until side issue #54 wires their heartbeats. Legacy rollup-name rows in `agent_heartbeats` from before #49 will age out naturally via the merge at `ops.js:466` (stale timestamps surface alongside but don't block the canonical names). |
| 56 | RESOLVED (duplicate) | Migrate Uploadcare File Uploader 3.x → Blocks v1.x | ad-hoc (2026-05-12) | — | — | **Duplicate of #52** — same title in input list. Carrying #52's status: promoted to Theme 13, superseded by direct-to-R2 migration. Row kept (vs deleted) so #56 stays as a stable identifier for cross-references. |
| 57 | RESOLVED | Sub-issue C2 sweep — bare `.toLocaleString()` cleanup in admin/superadmin/doctor-analytics views | Theme 10b Sub-issue C (commit `30ea413`) follow-up | P3 | None | Resolved in this commit. Swept 37 bare `.toLocaleString()` / `.toLocaleDateString()` / `.toLocaleTimeString()` callsites across 11 admin/superadmin/doctor-analytics views onto explicit `'en-GB'` locale (preserves any existing options object). T3 lint test (`tests/lint/no-bare-tolocalestring.test.js`) allowlist contracted from admin/superadmin/doctor-analytics surfaces down to just `case_lifecycle.js` (timezone-fixed Cairo-time computation) and `formatNumber.js` (the helper itself). T3 now enforces "no bare callsites" across all 162 view + job files. `admin_reviews.ejs` and `superadmin_instagram.ejs` already had explicit `'en-US'` (not bare); not in this sweep. |
| 58 | HOLD | Visual QA decision: keep option-1 font swap OR migrate to unicode-range only (Theme 10b Phase 1 follow-up) | Theme 10b Sub-issue F (commit `6456299`) follow-up | P3 | Internal-policy (visual QA gate) | **Phase 1 deviation note "tokens are dead code" was wrong.** Investigation 2026-05-12 surfaced 5 live callsites: `patient-tokens.css:200` (`html[dir="rtl"] .p-portal { font-family: var(--font-arabic), var(--font-sans); }`), `doctor-profile.css:51` + `doctor-profile.css:424` (bio area dir="rtl"), `doctor-prescribe.css:66` (local re-export), `public/styles.css:203` (`.arabic-text` utility). Tokens implement OQ-7's rejected option-1 path (Arabic-first font swap under RTL). After Phase 1's unicode-range mix in `--font-sans`, option-1 is **redundant but not dead** — base stack now handles Arabic glyphs natively. Decision deferred to post-launch: (A) close as "won't fix" if option-1 produces a noticeable improvement vs unicode-range-only, OR (B) ship cleanup (replace 4 callsite selectors with `var(--font-sans)`, then remove tokens) if visual outcome under AR is identical. Hold until AR rendering is observable in production. |
| 59 | OPEN | Canonical `chev-end.ejs` / `arrow-end.ejs` EJS partials if directional-SVG count grows past ~30 | Theme 10b Sub-issue B (commit `8feef60`) follow-up | P3 | None | Audit §4 Sub-issue B proposed 4 canonical EJS partials (`chev-end.ejs`, `chev-start.ejs`, `arrow-end.ejs`, `arrow-start.ejs`). Phase 4 deferred — at the current 9 directional-SVG callsites, 4 partials would be over-engineering. If a future theme adds enough new directional SVGs that the count grows past ~30 (when partial-vs-direct-class ratio crosses break-even), revive this work and refactor every callsite onto the partials. T2 lint (`tests/lint/directional-svgs-have-flip-class.test.js`) keeps the invariant healthy in the interim. |
| 60 | OPEN | Sub-issue A2 cleanup — `left:`/`right:` positioning logicalisation + redundant `[dir=rtl]` override deletion (post-visual-QA in production) | Theme 10b Sub-issue A (commit `7d9de1c`) follow-up | P2 | None | Phase 5 mechanically logicalised 76 margin/padding/border-l/r + text-align decls across 12 CSS files but intentionally skipped: (a) `left:`/`right:` positioning props (~20 callsites; semantic intent varies between inline-axis pin and decorative offset), (b) redundant `[dir=rtl]` override deletion (~30 overrides became semantically redundant but harmless), (c) files with existing RTL overrides not in §4's enumeration (`annotator.css`, `doctor-case-detail.css`, `doctor-dashboard.css`, `doctor-guide.css`, `doctor-profile.css`, `doctor-prescriptions.css`, `doctor-portal-v2.css`, `patient-portal-v2.css`, `patient-tokens.css`). ~50 callsites total. **Severity P2 (was P3 in #8c92f0c)** because the sidebar pin and decorative-offset semantic split is the kind of latent bug that produces "looks weird in production" — needs per-callsite review post-visual-QA in production. |
| 61 | OPEN | Migration 051 `ON CONFLICT (id) DO NOTHING` guards the wrong constraint | Theme 13 Phase 5 dev-DB diagnosis (2026-05-14) | P1 | None | `src/migrations/051_insert_lab_panels.sql:40` uses `ON CONFLICT (id) DO NOTHING` for idempotency, but the unique constraint that actually fires is `services_specialty_name_unique` on `(specialty_id, name)`. If an earlier seed inserted the same `(specialty_id, name)` pair with a different `id` (which happened in the local dev DB — two conflicting `svc-lab-pathology-*` rows for `Hormone Panel Review` + `Autoimmune Panel Review`), the whole migration transaction crashes with `duplicate key value violates unique constraint`. **Production likely past it** — prod ran 051 cleanly because no prior seed conflicted; this is a one-time issue surfaced by re-running migrations against a stale local DB. **Fix:** change to `ON CONFLICT (specialty_id, name) DO NOTHING` so the guard targets the constraint that can actually fire. Same audit should sweep 052/053/054 + any other `services` INSERTs that use the wrong-target pattern. Local dev unblocked 2026-05-12 via a one-shot DELETE of the 2 conflicting `svc-lab-pathology-*` rows; the migration fix is the durable resolution. |
| 62 | OPEN | `tests/run.js` async IIFE tests don't count in the totals line | Theme 13 Phase 5 (2026-05-14) | P2 | None | The runner `require()`s each test file synchronously and prints `Passed/Failed/Skipped` totals immediately after the loop. Async IIFE-pattern tests (every Theme 13 boot-server test, plus several pre-existing async tests) print their `t.pass()` / `t.fail()` lines AFTER the totals — so the totals understate real coverage. The runner exits via `process.exit(1)` on any failure (including the unrelated pre-existing `mark-paid earnings_writer comment` failure), killing async work mid-flight. **Net effect during Theme 13 Phase 5:** 9 new passing async assertions don't show in the suite count, but all 9 pass when run individually (verified). **Fix options:** (a) require test files to export a Promise the runner awaits, or (b) install a global tracker the runner waits on (`Promise.all(global._testRunner._pending)` after the for-loop). Option (b) is non-breaking — doesn't require touching every existing test file. |
| 63 | OPEN | Doctor has no UI surface for message-attached files | Theme 13 C2 scoping (2026-05-14) | P3 | None | `messages.ejs` (the unified message-thread view used by doctors) is text-only — confirmed by `grep -n 'file' messages.ejs` returning zero file-related matches. `portal_doctor_case.ejs` (doctor case detail) doesn't list `order_additional_files` either — confirmed by exhaustive grep across the views layer. The mirror to `order_additional_files` is written by `patient.js:3147` when a patient sends a message with an attachment, but **no doctor-side reader consumes it**. Today this is masked because production has 0 messages and 0 order_additional_files (verified via prod query 2026-05-14). It surfaces the moment a patient actually attaches something — the doctor would receive a notification ("Patient sent a message") but couldn't view the attached file in any portal page. **Fix options:** (a) add file-list rendering to `messages.ejs` for file-typed messages; (b) extend `portal_doctor_case.ejs` Documents tab to merge `order_additional_files` like patient_order.ejs does; (c) both. **Out of Theme 13 C2 scope** — Theme 13 retires Uploadcare for the WRITE path; the doctor-side read gap is independent and pre-existing. |
| 64 | RESOLVED | `o.reference_code` → `o.reference_id` rename on `orders_active` SELECTs | ad-hoc (2026-05-14 production fire) | P1 | None | Resolved at `028d480`. Production error `column o.reference_code does not exist` firing 8+ times on `/dashboard` GET in the last 24h, plus latent failures on order detail, request-refund GET/POST, report view, and the report-content helper. Migration `043_codify_mobile_api_schema.sql:44` added `orders.reference_id` as the canonical "TSH-XXX" case number; `cases.reference_code` (migration 001:93) was retained but `orders.reference_code` never existed post-043. Six SELECTs renamed: `patient.js:1012` (/dashboard SAFE_ORDER_COLS), `patient.js:2589` (order-detail SAFE_ORDER_COLS), `patient.js:2852` (request-refund GET), `patient.js:2903` (request-refund POST), `reports.js:63` (report page), `helpers/load-report-content.js:20` (report content loader). Sites already using `COALESCE(o.reference_id, c.reference_code)` (patient.js:3559, superadmin.js:1638/1810/2768, case_lifecycle.js:49) left alone. Cases-table INSERTs (intake.js:282, api/cases_intake.js:112) untouched — `cases.reference_code` is the right column there. |
| 65 | RESOLVED | `additional_files_requested` / `uploads_locked` integer literals on `BOOLEAN` columns | ad-hoc (2026-05-14 production fire) | P1 | None | Resolved at `63a6fa0`. Production error `column "additional_files_requested" is of type boolean but expression is of type integer` on `POST /portal/patient/orders/:id/upload` — blocking the wizard upload state-clearing UPDATE, leaving cases stuck in "waiting on patient" from the doctor's view. Migration `001_initial_tables.sql:53-54` defines both columns as `BOOLEAN DEFAULT false`; PostgreSQL does not auto-cast 0/1 to boolean. SQLite-legacy literals migrated to `true`/`false`: `patient.js:3495,3496,3504` (hot UPDATEs in the upload-complete handler), `patient.js:3452` (legacy `Number(...) === 1` read swapped for `=== true`), and 4 latent INSERTs at `patient.js:2102/2115/2330/2346` (`0, 0` → `false, false` for the `(uploads_locked, additional_files_requested)` value pair — would have fired as soon as those code paths ran). `order_flow.js:768`, `intake.js:252`, `public_orders.js:115` already used the right form — proof the canonical idiom was known elsewhere. Column type is correct (boolean is the right semantic — a flag, not a count); fix is on the value side, not the schema side. |
| 66 | OPEN | `TEMPLATE_TO_EMAIL` map missing entries for `payment_reminder_30m / _6h / _24h` | ad-hoc (2026-05-14, surfaced during #64/#65 investigation) | P2 | None | `src/notification_worker.js:88` looks up the email template via `TEMPLATE_TO_EMAIL[notification.template]`; the map at lines 32-74 has no entries for `payment_reminder_30m`, `payment_reminder_6h`, or `payment_reminder_24h`. But `notify.js:86-88` whitelists those templates as enabled and `case_lifecycle.js:469` queues them. Three payment reminder emails dropped on the floor every time a reminder fires, with `no_email_template_mapping_for_payment_reminder_*` written to `error_logs`. WhatsApp + in-app channels still fire (where Meta is unblocked). **Fix options:** (a) add three rows to `TEMPLATE_TO_EMAIL` pointing at an existing template like `payment-reminder` (verify the EJS template exists first), or (b) gate the email channel per template in `notify.js` if these are intentionally not email-bearing — letting the worker hit the "unsupported" branch is the noisy outcome. Surfaced from Claude Code during #64/#65 production-fire investigation. |
| 76 | OPEN | Inconsistent `code` column in `services` table — 42 of 99 rows NULL, 57 mirror `id` | Theme 14 catalog cleanup pre-flight (2026-05-14) | P3 | None | The `services.code` column is half-populated in prod: 57 of 156 rows have `code = id` (the established convention when set), 42 are NULL, with a long tail of UUID-style legacy rows accounting for the rest. The convention going forward is `code = id` and migration 057 (specialty catalog cleanup) sets it explicitly for the 6 new consultation rows. **Backfill recommended:** `UPDATE services SET code = id WHERE code IS NULL`. Safe + additive — `code` is currently consumed only by Paymob payment_link resolution at `src/routes/payments.js` (which falls back to `id` when null, so the backfill is invisible to existing behaviour but tightens future invariants). Surfaced during Theme 14 catalog cleanup pre-flight investigation. |
| 77 | RESOLVED | Haiku classifier output wraps JSON in markdown fences despite prompt instruction | Theme 14 Phase 3 polish walkthrough (2026-05-16) | P1 | None | Resolved in this commit. Production walkthrough at 2026-05-16T16:26:24Z surfaced a perfectly-shaped locked-tier (0.95) classifier output for order `b25578e6-c853-4ec3-8da2-2aad368f566a` — Cardiology + `card_ecg_12lead` for "Exertional chest pain with ECG attachment" — that was silently rejected because Haiku wrapped the JSON in ```` ```json … ``` ```` despite the SYSTEM_PROMPT's explicit "no markdown fences" instruction. `JSON.parse` threw, the try/catch in `src/routes/patient.js` Step 2 POST logged + swallowed, no `specialty_classifications` row was written, Step 3 fell back to the legacy supply-blind grid (which is the screenshot Ziad surfaced). Root cause: prompt-only enforcement is unreliable for the "no markdown" guardrail — Claude's RLHF defaults to wrapping JSON in fences for readability. **Fix:** defensive parser-side strip in `src/services/specialty_classifier.js`. New regex extracts JSON from ```` ```json … ``` ```` or ```` ``` … ``` ```` wrappers if present; passes bare JSON through unchanged. Behaviour locked with 5 new tests in `tests/unit/specialty-classifier.test.js` covering: json-tagged fence (the prod variant), no-tag fence, trailing whitespace, no-trailing-newline, and bare JSON regression guard. |
| 78 | RESOLVED | `service_regional_prices` EG drift on 4 v4 stragglers | Theme 14 Phase 3 polish walkthrough (2026-05-16) | P1 | None | Resolved in this commit (migration 059). Theme 14 Phase 3 polish walkthrough surfaced "12-Lead ECG Interpretation" rendering at EGP 575 instead of the canonical EGP 1,250. Root cause: migrations 051/052/053 (the v4 launch reset) bumped 5 stragglers to 1,250 EGP on the canonical `services.base_price` column but missed the matching `service_regional_prices` rows for country_code='EG'. The patient.js GET handler at Step 3 hydrates services with `COALESCE(cp.tashkheesa_price, sv.base_price)` so the stale regional row wins when present. **Affected:** `card_ecg_12lead` (575 → 1250), `card_rhythm_strip` (575 → 1250), `rad_cxr_review` (633 → 1250), `lab_cytology` (1035 → 1250). The remaining 139 visible services are either consistent with canonical (34 rows) or have no regional row at all (105 rows) — narrow drift, not table-wide. Forensic snapshot of the pre-migration state preserved in `docs/audits/PRICING_DRIFT_2026-05-16.md` (143-row diff). Post-condition guard in the migration asserts exactly 4 rows aligned at 1,250 EGP after the UPDATE — txn rolls back if the targeted set drifts. |
| 73 | RESOLVED | `/portal/patient` + `/portal/patient/orders` bare collection URLs 404 (no `router.get` for them) | Patient walkthrough triage (2026-05-17) | P3 | None | Resolved at `62d11aa`. Pre-flight investigation confirmed the `/portal/patient/*` namespace is NOT legacy — it holds ~30 active routes across 8 route files (patient.js, onboarding.js, medical_records.js, referrals.js, reviews.js, appointments.js, prescriptions.js, patient_files.js) including the load-bearing `/portal/patient/orders/:id`, `/portal/patient/payment-return` (Paymob callback per `docs/launch-checklist.md`), `/portal/patient/files` (upload endpoint), `/portal/patient/onboarding/*`. Only the bare collection URLs `/portal/patient` and `/portal/patient/orders` had no handler. Added two 302 redirects in `src/routes/patient.js` next to the existing `/portal/patient/orders/new` → `/patient/new-case` 301 alias. Both redirects gate behind `requireRole('patient')` so unauthenticated requests hit `/login` first (standard product behavior). |
| 74 | RESOLVED | `/signup` returns 404; canonical is `/register` | Patient walkthrough triage (2026-05-17) | P3 | None | Resolved at `90a4adb`. Single-line 302 redirect added in `src/routes/auth.js` above the `GET /register` handler. Type-302 (temporary) so browsers don't permanently cache the alias if the canonical route ever moves. Pre-flight grep confirmed zero `/signup` references in `src/views/` or `public/` — the redirect is purely defensive for users who land on the path via mental-model from other apps. |
| 75 | RESOLVED | Onboarding form inputs missing `name=""` attrs (autofill + graceful-degradation gap) | Patient walkthrough triage (2026-05-17) | P3 | None | Resolved in this commit. All 10 form inputs in `src/views/patient_onboarding.ejs` had `id="ob-*"` but zero `name=""` attributes. Data persists today via the inline JS at `:305+` that reads via `getElementById('ob-*').value` and POSTs JSON via `fetch()` to `/portal/patient/onboarding/{profile,medical-history,complete,skip}` — `getElementById` ignores `name`, so the JS happy path is unaffected. The gap matters for: (a) browser autofill (saved name/phone won't populate without `name`+`autocomplete`), and (b) graceful degradation if JS ever fails (form would submit traditionally and lose every field — currently no fallback). Added `name="…"` matching each handler's `req.body.<field>` expectation (`name`, `phone`, `date_of_birth`, `gender`, `preferred_lang`, `known_conditions`, `current_medications`, `allergies`, `previous_surgeries`, `family_history`) plus `autocomplete="…"` tokens (`name`, `tel`, `bday`, `sex`, `language`) on the visible inputs. No JS change, no POST handler change — pure attr addition on the EJS markup. JS-path regression check confirmed inline: `getElementById('ob-*').value` reads remain identical, the fetch call bodies are byte-identical, no behavior change in the success path. |
| 79 | RESOLVED | Hide Psychiatry from the patient wizard | ad-hoc (2026-05-17) | P2 | None | Resolved in this commit (migration 060). Per Ziad: Psychiatry doesn't launch at Shifa, so the specialty must not appear in the wizard grid or in the AI classifier's candidate enum. Migration 060 flips two flags: `specialties.is_visible=false` for `spec-psychiatry` and `services.is_visible=false` for `svc-psychiatry-consultation` (the sole psychiatry service, added in migration 057). No wizard or classifier code change needed — the patient grid queries already filter on `COALESCE(is_visible, true) = true` (`src/routes/patient.js:1586,1593,2159,2172,2367,2380`) and the classifier-enum builder at `src/routes/patient.js:1614` defensively drops specialties with zero visible services. Hiding the service alongside the specialty matches the migration-057 precedent (services are independent of `specialties.is_visible` — no implicit cascade) and keeps the service unreachable even if the specialty is force-shown later. **End state:** 20 visible specialties (was 21 post-057), all bookable; 0 visible psychiatry services. Post-condition guards in the migration assert both invariants atomically. |
| 80 | RESOLVED | Seed classifier threshold rows in `admin_settings` (Theme 14 Phase 4 piece 1) | Theme 14 Phase 4 scoping (2026-05-17) | P2 | None | Resolved in this commit (migration 061). Theme 14 Phase 4 makes the three classifier confidence thresholds (locked / auto / minimum) live-tunable from the superadmin UI without a deploy. Piece 1 is the prerequisite: seed three rows into the existing `admin_settings` table (schema unchanged — `key` PK / `value` text / `updated_by` nullable / `updated_at` default now()). Keys align with the canonical reference at `src/services/specialty_classifier.js:25`: `classifier_threshold_locked = '0.95'`, `classifier_threshold_auto = '0.85'`, `classifier_threshold_minimum = '0.55'`. Values seeded as TEXT (matches column type); the helper module (piece 2) parses to Number with finite + `[0,1]` guard. **Idempotency:** `ON CONFLICT (key) DO NOTHING` — re-running the migration never trams a superadmin's tuned value with the seed default. Verified in dry-run by attempting a second INSERT with value `'0.99'` against `classifier_threshold_locked` — returned `INSERT 0 0`, value stayed `0.95`. Post-condition guard asserts all 3 keys exist atomically. The pre-existing `auto_assign_enabled = 'false'` row (seeded ad-hoc 2026-03-23) is untouched. **Pieces 2-4 follow:** helper module + unit tests → refactor 3 hardcode sites at `patient.js:1427`, `patient.js:1717`, `patient_new_case.ejs:339-341` → superadmin UI at `/superadmin/settings` with integration tests + nav link. |
| 81 | RESOLVED | Bell icon missing on patient topbar (badge floats with no glyph) | Patient dashboard walkthrough (2026-05-17) | P3 | None | Resolved in this commit. The `bell` entry in the inline-SVG path map at `src/views/partials/patient/icon.ejs:18` rendered malformed geometry — the path `'M15 17H9a6 6 0 016-6V8a3 3 0 00-6 0v3a6 6 0 01-3 5.2M10 21h4'` produces an open arc fragment with a stray clapper, not a recognizable bell shape inside the 24×24 viewBox. The notification badge (`.p-bell-dot` at `partials/patient/topbar.ejs:31-32`, populated to `'9+'` by `partials/patient/foot.ejs:138`) still renders correctly — so patients saw a floating "9+" with no icon beside it. Replaced with the canonical Feather bell path already used at `partials/patient_sidebar.ejs:38`: `'M18 8A6 6 0 006 8c0 7-3 9-3 9h18s-3-2-3-9M13.73 21a2 2 0 01-3.46 0'` — two subpaths (bell body + clapper) chained via the `M` operator in a single `d` attribute, matching the single-`<path>` shape of the icon helper. Smoke-tested by rendering the partial standalone and asserting two subpaths in the output. Pure one-line edit; no behaviour or API change. |
| 82 | RESOLVED | Patient sidebar "Messages" 404 — wrong href, no `/portal/patient/messages` handler | Patient dashboard walkthrough (2026-05-17) | P2 | None | Resolved in this commit. The new patient sidebar at `partials/patient/sidebar.ejs:49` and the mobile bottom tabbar at `partials/patient/mobile-tabbar.ejs:31` both pointed at `/portal/patient/messages`, but the canonical messages inbox handler lives at `/portal/messages` (`src/routes/messaging.js:72`, shared across patient + doctor roles) — there's no `/portal/patient/messages` route, so clicks 404'd. The doctor-side counterpart at `src/routes/doctor.js:915` had already established the alias pattern (`/portal/doctor/messages` → `/portal/messages`); the patient-side analogue was never added. **Fix (two-part):** (1) Updated both sidebar + mobile-tabbar hrefs to `/portal/messages` directly — eliminates the redirect hop for in-app navigation. (2) Added a defensive 302 alias in `src/routes/patient.js` next to the existing `/portal/patient` + `/portal/patient/orders` aliases (the #73 fixes): `router.get('/portal/patient/messages', requireRole('patient'), (req, res) => res.redirect(302, '/portal/messages'))`. Catches any future bookmark, external link, or partial that still uses the wrong URL. Type-302 (not 301) matches the patient-module convention (`patient.js:1474-1475`) and keeps a future dedicated `/portal/patient/messages` page reversible. |
| 87 | OPEN | Chrome inconsistency between `/portal/messages` (older sidebar) and `/dashboard` (newer sidebar) | Side issue #82 investigation (2026-05-17) | P3 | None | `src/views/messages.ejs:59` includes the older `partials/patient_sidebar.ejs` (the pre-Theme-10b chrome — `.portal-sidebar` with role-anchored layout), while `/dashboard` and the rest of the patient portal use the newer `partials/patient/sidebar.ejs` (Theme 10b chrome — `.p-sidebar` with the new design-system tokens). After #82 lands, patients clicking Messages from the new dashboard sidebar land on `/portal/messages` and see a different sidebar style — same nav items but different visual weight, spacing, and active-state treatment. Not a functional break (every nav target remains reachable), but jarring on a side-by-side click-through. **Fix options:** (a) port `messages.ejs` to the new chrome by swapping the include + adding the head/foot wrapping that the new system requires; (b) wholesale rewrite of `messages.ejs` to match the new dashboard's visual language (larger scope — affects both patient + doctor since the view is shared). Out of scope for #82 because the chrome migration is a separate visual-system decision, and #82's surgical fix (href + redirect) closes the 404 without touching the view layer. Surfaced during #82 investigation. |
| 85 | RESOLVED | Align "24–72 hours" + "Shifa, El Tagamoa" copy with launch reality across 7 patient-facing surfaces | Patient dashboard walkthrough (2026-05-17) | P2 | None | Resolved in this commit. The dashboard walkthrough surfaced two copy inaccuracies: (a) "TURNAROUND: 24–72 hours" — wider than what the wizard actually delivers (`src/services/wizard_pricing.js:110` hardcodes Standard=48h / VIP=18h / Urgent=4h); (b) "PARTNER HOSPITAL: Shifa, El Tagamoa" — Shifa Hospital Group has multiple branches (Tagammo + Sherouk), so the single-branch phrasing reads as a smaller affiliation than it is. **Scope:** the 4 originally-scoped files (`patient_dashboard.ejs`, `app_landing.ejs`, `services.ejs`, `index.ejs`) plus 3 patient-flow surfaces surfaced by the second-sweep grep (`patient_order.ejs` post-purchase order detail, `patient_payment_success.ejs` confirmation, `coming_soon.ejs` pre-launch landing) — 7 files total. The 3 blog files (`blog_how_tashkheesa_works.ejs`, `blog_when_to_get_second_opinion.ejs`, `blog_index.ejs`) are intentionally deferred to a separate editorial pass per #88 because they include JSON-LD HowToStep structured-data + SEO copy that warrants a review independent of the bug-fix batch. **Phrasing (Ziad-locked):** EN long-form = "Written opinion in 48 hours standard, as fast as 4 hours — or your money back." / AR = "رأي مكتوب خلال 48 ساعة قياسي، وحتى 4 ساعات للحالات العاجلة — أو نعيد المبلغ." Short-form stats row: EN "48h standard · 4h urgent" / AR "48 ساعة قياسي · 4 ساعات عاجل". Timeline mid-dot tweak applied at `patient_dashboard.ejs:392`. Partner-hospital phrasing: EN "Shifa Hospital Group · Tagammo & Sherouk branches" / AR "مجموعة مستشفيات الشفاء · فرعا التجمع والشروق" — single edit at `patient_dashboard.ejs:410` (the only site that named a specific branch; the trust-badge sites at `app_landing.ejs:137` + `partials/patient/sidebar.ejs:66,71` say "Shifa Hospital partner" / "بالتعاون مع مستشفى الشفاء" — not misleading, intentionally left as-is). Acceptance-SLA "refund within 48 hours" copy elsewhere in the wizard is unchanged — that's a separate guarantee about doctor acceptance, not turnaround. |
| 86 | OPEN | SLA hours drift — `services.sla_hours` column (72) vs `wizard_pricing.js` helper (48 standard) | Patient dashboard walkthrough investigation (2026-05-17) | P3 | None | Surfaced while fixing #85 (turnaround copy alignment). Migration 057 creates the 6 baseline consultation services with `sla_hours = 72` (`src/migrations/057_theme14_specialty_catalog_cleanup.sql:78,89,100,111,122,133`), but `src/services/wizard_pricing.js:110` hardcodes the tier-derived SLA as `tier === 'urgent' ? 4 : tier === 'vip' ? 18 : 48`. The wizard view at `patient_new_case.ejs:384,587,655` reads the per-service column with a 72h fallback. Net result: Step 3 service-card subtext renders "Standard 72h turnaround" (from the DB column), while the canonical wizard helper says Standard=48h. The dashboard / homepage / patient flow copy now says 48h (per #85), so the wizard service-card "72h" line directly contradicts the dashboard reassure card. **Fix options:** (a) Update the DB rows so `services.sla_hours = 48` for the 6 Standard-tier consultation services (and verify the rest of the 156 service rows); (b) Drop the per-service `sla_hours` column entirely and let `wizard_pricing.js` be the single source of truth; (c) Keep per-service `sla_hours` but rewrite the helper to read FROM the column instead of hardcoding tier values. Recommend (c) — preserves per-service flexibility (e.g., a future Lab panel might genuinely take 72h) while removing the helper's hardcoded fallback. Out of scope for #85 because it changes runtime behaviour (Step 3 card sub-line + Step 4 SLA-disclaimer modal), not just copy. |
| 88 | OPEN | Blog copy still references stale "24–72 hours" turnaround across 3 blog files (incl. JSON-LD HowToStep) | Side issue #85 scope-expansion grep (2026-05-17) | P3 | None | Deferred from the #85 batch because the blog files surface "24–72 hours" inside SEO copy (excerpt + meta description fields) AND JSON-LD HowToStep structured data at `src/views/blog_how_tashkheesa_works.ejs:29`. A schema rewrite without an editorial pass risks search-result snippet churn or Google Search Console structured-data warnings. **Affected files (15+ instances total):** `src/views/blog_how_tashkheesa_works.ejs` (8 instances at lines 16, 17, 29, 88, 112, 136, 144, 168, 192 — incl. JSON-LD HowToStep at line 29), `src/views/blog_when_to_get_second_opinion.ejs` (5+ instances at lines 110, 120, 128, 164, 174, 182), `src/views/blog_index.ejs` (2 instances at lines 28, 29 — excerpts shown on the blog listing). Same canonical phrasing as #85: EN "48 hours standard, as fast as 4 hours" / AR "48 ساعة قياسي، وحتى 4 ساعات للحالات العاجلة". When this gets picked up, also re-validate the JSON-LD against Google's Rich Results Test to make sure the schema still parses cleanly. |

## How to add to this ledger

1. Pick the next unused number.
2. Append a row to the table. Keep the columns in the schema order.
3. If the issue resolves, change `Status` to `RESOLVED` and put the commit SHA(s) in the `Notes` column.
4. If the issue is bigger than a side issue (multi-week, multi-PR), promote it to a Theme and update the `Notes` to reference the Theme number. Side-issue row stays in the ledger as `DEFERRED` with a pointer.
5. If a side issue turns out to be a duplicate, mark it `OPEN (duplicate)` and point at the canonical row in `Notes`. Don't renumber — leaves git blame stable.
