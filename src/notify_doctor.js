module.exports = {
  sendDoctorEmail(toEmail, subject, message) {
    // Placeholder for real email integration
    console.log('[EMAIL to doctor]', toEmail, subject, message);
  },
  sendDoctorWhatsApp(toNumber, message) {
    // Placeholder for real WhatsApp integration
    console.log('[WHATSAPP to doctor]', toNumber, message);
  }
};
