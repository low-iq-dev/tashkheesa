<!DOCTYPE html>
<html lang="<%= (lang || 'en') %>" dir="<%= (dir || 'ltr') %>">
<head>
  <meta charset="utf-8" />
  <title>Tashkheesa – Patient Portal</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <header>
    <div class="brand">
      <div class="brand-logo">T</div>
      <div>Patient Portal</div>
    </div>
    <form action="/logout" method="post">
      <%- (typeof csrfField === 'function') ? csrfField() : '' %>
      <button class="btn btn-outline" type="submit">Logout</button>
    </form>
  </header>

  <main>
    <div class="card">
      <h1>Hi, <%= (user && user.name) || 'Patient' %> 👋</h1>
      <p class="subtitle">
        The detailed patient portal (orders list, reports, timelines, file uploads)
        is currently being rebuilt and is temporarily disabled.
      </p>
      <p class="muted">
        Your cases and medical data are still safely stored in the system.
        This page is only a temporary placeholder.
      </p>
      <p class="muted">
        Please contact the Tashkheesa team or Shifa Hospital directly if you need an update on any report.
      </p>
      <div style="margin-top:16px;">
        <form action="/logout" method="post" style="margin:0;">
          <%- (typeof csrfField === 'function') ? csrfField() : '' %>
          <button class="btn btn-primary" type="submit">Logout</button>
        </form>
      </div>
    </div>
  </main>
</body>
</html>
