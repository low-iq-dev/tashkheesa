<%- include('partials/header', { title: 'order_confirmation.ejs' }) %>
<file name=order_flow.js path=/Users/ziadelwahsh/Desktop/tashkheesa-portal/src/routes>
const express = require('express');
const router = express.Router();

router.post('/order/confirmation', (req, res) => {
  const { paymentStatus, reference, slaType, slaDeadline } = req.body;

  if (paymentStatus === 'paid') {
    res.render('order_confirmation', {
      reference,
      slaType,
      slaDeadline,
      supportEmail: 'support@tashkheesa.com'
    });
  } else {
    res.render('order_confirmation', {
      reference,
      slaType,
      slaDeadline,
      supportEmail: 'support@tashkheesa.com'
    });
  }
});

module.exports = router;
</file>