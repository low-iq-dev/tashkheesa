<%
  // View-callable entry point. Each superadmin view starts with the
  // header include and ends with the footer include. Pass-through to
  // layouts/superadmin.ejs so the call site mirrors the existing
  // portal pattern.
%><%- include('../../layouts/superadmin', locals) %>
